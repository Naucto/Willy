# Willy — Self-Hosted Mini-PaaS (master plan)

> Rework of `Naucto/Docker-Synchronizer` into **Willy**, a general self-hosted mini-PaaS.
> This file is the master plan. On implementation start it is copied to `docs/plans/0001-willy.md`
> and each phase below becomes (potentially) its own working session.

## Context

Today this repo is a single-purpose Python service: a Flask webhook listener (`hosting.py`) drives
`controller.py`, which pulls git submodules (`dev_environment` = the Naucto NestJS backend +
React/Vite frontend), runs `docker-compose build`, then `down`/`up`. It deploys **exactly one** app
to **one hardcoded domain** (`SSL_TARGET_DOMAIN`); TLS is **manual** (pre-existing certs mounted into
`fsouza/docker-ssl-proxy` sidecars); there is **no webhook authentication**; redeploys cause **full
downtime**; it is installed as a systemd service via `install.sh`.

We are **resetting** this repository into **Willy** (a nod to Docker's whale → "Free Willy"): a
general mini-PaaS that deploys many arbitrary git apps — each on its own domain/subdomain **or with
no domain at all** (background workers / cron jobs) — with per-deployment encrypted env vars,
automatic Let's Encrypt certificates via OVH, OVH DNS record management, per-container console + log
journaling, volume/DB/S3 backup-restore, optional GitHub webhook auto-deploy, and a near-one-shot
remote provisioner that turns a fresh Debian VPS into a running Willy host. The existing Naucto app
stops being baked in and becomes *just one deployment Willy manages*; the GitHub↔Jira synchronizer
and the Epitech repository-crawler mirror become **domain-less worker deployments**.

Why: the current system is a hardcoded, single-app, manual-TLS, full-downtime deployer. Naucto needs
to host multiple services (web apps *and* headless workers) across domains with self-service
management, automated certs, and operational tooling (logs, console, backups) from a web panel,
provisionable onto a fresh VPS in essentially one command.

---

## Confirmed decisions

| Area | Decision |
|---|---|
| Scope | General mini-PaaS, many arbitrary apps. |
| Deploy source | Git repo + build: **Nixpacks** (auto-detect), explicit **Dockerfile**, **docker-compose** passthrough. |
| Deployment types | **WEB** (Traefik router + domain), **WORKER** (no domain/port, long-running), **CRON** (scheduled). Domain-less = WORKER/CRON. |
| Auto-deploy | Per-deployment GitHub webhook toggle (manual default), **HMAC verified**. |
| Env vars | Per-deployment, **AES-256-GCM encrypted at rest**, UI-editable, masked. |
| Domains | **Any domain/subdomain is configurable per app** — not limited to the panel's zone. Constraint: the FQDN must sit in an **OVH-managed zone** Willy's token can edit (the "OVH perimeter"), so DNS records + DNS-01 certs are automatic. Panel itself defaults to **`willy.naucto.net`** (configurable). |
| Edge / TLS | **Traefik v3.7** + ACME **DNS-01 via OVH**, issuing **per-domain certs across any OVH zone**; optional **per-zone wildcard** as an optimization where many subdomains share a zone (e.g. `*.willy.naucto.net`). Replaces certbot + `fsouza/docker-ssl-proxy`. |
| DNS | **OVH DNS record management** in-panel, **multi-zone** (create/point/update/delete A/CNAME/TXT in the correct zone for each app's FQDN). |
| Switching | **Health-checked swap with a brief interruption** (NOT strict zero-downtime). Build → start new → health-check → flip Traefik route → remove old; the **old version stays up until the new one is healthy**, so a broken build never replaces a working version. No side-by-side dual-serving / connection draining. Same model for WEB, WORKER/CRON, and compose. |
| Control plane | **NestJS + Drizzle ORM + PostgreSQL** (Drizzle replaces Prisma: schema-as-TS, typed queries, `drizzle-kit` SQL migrations — lighter, closer to SQL). Docker via **dockerode** through **docker-socket-proxy**. |
| Managed databases | **In scope, a Willy feature:** one-click managed DB instances (Postgres/MySQL/Redis/Mongo) Willy runs (container + named volume + generated creds on a private network), **attachable** to deployments (auto-injects the connection string as encrypted env), with first-class backup/restore. Apps can still bring their own DB via compose, but don't have to. |
| Build queue | **In-process** FIFO + global concurrency cap + per-deployment lock; job state persisted; behind a `BuildQueue` interface. No Redis in v1. |
| UI | **Custom React/Vite + MUI 9** dashboard (NOT AdminJS) — action/stream-heavy ops UX. MUI 9 is the latest (Apr 2026; unifies Material UI + MUI X versioning). |
| Runtime | **Node 24 LTS** for backend + frontend build images. |
| Quality / tooling | **Biome** (lint+format, one tool), **strict `tsc`**, **Vitest** (unit+integration), **Testcontainers** (real Postgres + Docker), **Playwright** (e2e), **lefthook** pre-commit, **GitHub Actions** CI. Maintainability-first; comments explain **why**, not how (short or none). |
| Provisioning | **`scripts/provision.sh`** (laptop) drives **`scripts/willy_deploy.sh`** (host) over SSH; verbs `provision`/`upgrade`/`doctor`/`backup`. Near-one-shot. |
| Local mode | **One-command local run** (`make dev` → compose override): base domain `*.localhost`, **mkcert** locally-trusted certs instead of OVH/ACME, no provisioner/DNS. Same code paths as prod; optional hot-reload. |
| Repo | Rename GitHub repo + local folder to **Willy** as first task; clean history reset. |

---

## Target architecture

All components run as containers in a **top-level infra `docker-compose.yml`** — this is
infrastructure, **not** managed through Willy's own deployment tables (Willy must not redeploy itself).

```
                       Internet (:80 → redirect :443)
                                 │
                          ┌──────▼──────┐  ACME DNS-01 ─► OVH API ─► Let's Encrypt (wildcard + per-domain)
                          │   Traefik   │  docker provider (labels, exposedByDefault=false) + file provider
                          └──────┬──────┘  acme.json (chmod 600) on volume
                    willy_edge network (Host-rule routing, NO host ports)
        ┌────────────┬───────────┼───────────────┬──────────────────────┐
   willy-web      willy-server   WEB app         WORKER app          CRON job
   (React+MUI 9)  (NestJS API)   (router+domain) (no router/domain)   (scheduled, no router)
                     │
        ┌────────────┼───────────────┬──────────────────────────┐
   docker-socket-proxy   PostgreSQL (Willy metadata)   willy_backups volume
   (least-privilege)
                     └─ dockerode (build/run/inspect/logs/exec/networks)
                     └─ Nixpacks / docker build / docker compose
                     └─ OVH API (DNS records)   └─ AES-256-GCM crypto   └─ in-proc build queue
```

**Network model (the key win):** one external network `willy_edge` that Traefik joins. WEB
containers join it and are routed **by Host rule, not host port** → zero port collisions (the old
compose publishes `443`/`1987` to the host, only works for one app). WORKER/CRON containers do **not**
join `willy_edge` and get no labels. Compose stacks keep their internal project network; only the
declared web service attaches to `willy_edge`. Everything namespaced by `deploymentId` (+ git SHA
during swaps) to avoid Traefik router shadowing.

---

## Repository / project layout (after reset)

```
/  (Willy monorepo, GitHub: Naucto/Willy, local: …/Naucto/Willy)
├── AGENTS.md              canonical agent/contributor guide  ← CLAUDE.md symlinks here
├── CLAUDE.md              → symlink to AGENTS.md
├── README.md             operator runbook + quickstart
├── docker-compose.yml        infra: traefik, docker-socket-proxy, willy-server, willy-web, postgres
├── docker-compose.local.yml  local override: *.localhost + mkcert certs, no OVH/ACME, optional watch mode
├── Makefile                  `make dev` (local), `make test`, `make lint`, etc.
├── .env.example          OVH creds, WILLY_MASTER_KEY, base domain, ACME email/CA, admin bootstrap, DB
├── scripts/
│   ├── provision.sh      laptop-side remote provisioner (verbs: provision/upgrade/doctor/backup)
│   └── willy_deploy.sh   host-side bootstrap (sudo-run, idempotent)
├── routing/              traefik.yml (static) + dynamic/ (middlewares, dashboard, tls)
├── backend/              NestJS control plane (data layer + modules)
├── frontend/             React + Vite + MUI 9 dashboard
└── docs/                 architecture, runbooks, ADRs
    └── plans/            0001-willy.md (this plan) + future plans
```

Repo reset is clean (decision #7): start fresh history — no `legacy/` folder, the old Python is not
carried forward. (Optional one-time safety: push a `pre-willy` tag/branch to the remote before resetting
so the old tree is recoverable from GitHub, even though it's not kept in the working tree.)

---

## Deployment type model (incl. domain-less)

`DeploymentType` enum drives everything downstream:
- **WEB** — has ≥1 Domain; gets Traefik router/service labels on `willy_edge`; health probe via HTTP
  path; zero-downtime swap. (e.g. Naucto frontend/backend.)
- **WORKER** — long-running, **no domain, no port, no Traefik labels, not on `willy_edge`**. Health =
  container running/healthcheck only. Swap = stop-old/start-new (brief gap acceptable; no traffic to
  drain). Full build/env/logs/console/backups/webhook support. (e.g. GitHub↔Jira synchronizer, Epitech
  repository-crawler mirror.)
- **CRON** — scheduled one-shot container on a cron expression (via `@nestjs/schedule`); no router;
  logs of each run retained. (Optional: a WORKER can declare an internal scheduler instead.)

The create-deployment wizard branches on type: WEB asks for port/domain/healthcheck; WORKER asks only
for run command/restart policy; CRON asks for the cron expression. Note: the two named apps (Jira sync,
repo crawler) still need their **own** code rewrites — out of scope here; Willy only needs to *host*
them as WORKER deployments.

---

## Data model (Drizzle, `backend/src/db/`)

Schema defined as TypeScript via Drizzle `pgTable`/`pgEnum` in `backend/src/db/schema/*.ts` (one file
per aggregate), barrel-exported. Migrations generated + applied with **`drizzle-kit`** (`drizzle-kit
generate` → versioned SQL in `backend/drizzle/`, `drizzle-kit migrate` to apply). IDs default to
`gen_random_uuid()` (pgcrypto) or cuid2; indexes/uniques expressed in the table builder.

**Enums** (`pgEnum`): `Role(ADMIN|OPERATOR|VIEWER)`, `DeploymentType(WEB|WORKER|CRON)`,
`BuildStrategy(NIXPACKS|DOCKERFILE|COMPOSE)`, `EnvScope(BUILD|RUNTIME|BOTH)`,
`ReleaseStatus(QUEUED|CLONING|BUILDING|HEALTHCHECKING|LIVE|SUPERSEDED|FAILED|ROLLEDBACK|INTERRUPTED)`,
`DeploymentState(CREATED|DEPLOYING|RUNNING|DEGRADED|STOPPED|ERROR)`,
`DomainType(SUBDOMAIN|CUSTOM_EXTERNAL|APEX)`, `CertStatus(NONE|PENDING|ISSUED|FAILED)`,
`DnsRecordType(A|AAAA|CNAME|TXT)`, `BackupKind(VOLUME_TAR|PG_DUMP|S3_SYNC)`,
`BackupStatus(PENDING|RUNNING|SUCCESS|FAILED)`, `RestartPolicy(NO|ON_FAILURE|ALWAYS|UNLESS_STOPPED)`,
`AuditAction(...)`.

**Models (key fields):**
- **User** — email unique, `passwordHash` (argon2id), `role`, `refreshTokenHash`.
- **Deployment** — `name` unique slug `^[a-z0-9][a-z0-9-]{0,40}$`, `type`, `gitUrl`, `gitRef`,
  `buildStrategy`, `dockerfilePath?`, `composeFilePath?`, `composeWebService?`, `runCommand?`,
  `cronExpr?`, `webServicePort?`, `healthCheckPath`, `autoDeploy`, `restartPolicy`, `memoryLimitMb?`,
  `nanoCpus?`, `state`, `activeReleaseId? @unique`. Relations: releases, envVars, domains,
  webhookSecret, backups.
- **Release** (== build job) — `deploymentId`, `gitSha`, `imageTag` (`willy/<name>:<sha>`), `status`,
  `containerId?`, `composeProject?`, `logPath?`, `errorMessage?`, timestamps. `@@index([deploymentId, createdAt])`.
- **Domain** — `fqdn @unique`, `deploymentId`, `type`, `certStatus`, `certResolver`, `isPrimary`.
- **DnsRecord** — `zone`, `subDomain` (`""`=apex, `*`=wildcard), `type`, `target`, `ttl`,
  `ovhRecordId?`, `managedByWilly`, `deploymentId?`. `@@unique([zone, subDomain, type])`.
- **EnvVar** — `deploymentId`, `key`, `cipherText`, `nonce`, `authTag`, `keyVersion`, `scope`,
  `isSecret`. `@@unique([deploymentId, key])`. Plaintext never stored/logged.
- **WebhookSecret** — `deploymentId @unique`, encrypted secret triplet, `provider`, `lastDeliveryId?`.
- **GitCredential** — auth for cloning **private app repos** at build time (distinct from Willy's own
  host-level deploy key set at provision time). `kind` (`PUBLIC|GITHUB_APP|DEPLOY_KEY|PAT`), encrypted
  payload (App installation id / private key, or per-repo SSH key, or token), optional `deploymentId`
  (or shared). **Recommended primary: a GitHub App** installed on the org → short-lived per-clone
  installation tokens scoped to selected repos (no long-lived secrets, no per-repo key juggling);
  per-repo **deploy keys** and **PAT** as fallbacks. Stored encrypted via the same CryptoService as env vars.
- **Database** (managed DB instance) — `name @unique`, `engine` (`POSTGRES|MYSQL|REDIS|MONGO`), `version`,
  `containerId?`, `volumeName`, `networkName` (private), encrypted credential triplet + `connStringEnvKey`,
  `state`, `memoryLimitMb?`. **DatabaseAttachment** — `databaseId` × `deploymentId` (m:n), `injectPrefix`
  (env var prefix); attaching injects the encrypted connection string into the deployment's runtime env.
- **Backup** + **BackupSchedule** — kind/status/target/location/sizeBytes/checksum/offsiteUrl;
  schedule: `cron`, `retention`, `enabled`, `lastRunAt`.
- **AuditLog** — append-only; `actorId?`, `action`, `targetType/Id`, `metadata Json` (secret-scrubbed),
  `ip`, `createdAt`.

---

## Control plane (`backend/`) — module map

Mirror existing Naucto NestJS conventions (Nest 11, `@nestjs/config` global, class-validator DTOs,
`@nestjs/swagger`, `@nestjs/schedule`, raw `ws` via `server.on("upgrade")` as in
`collab/signaling/signal.ts`) — but swap Prisma for **Drizzle ORM** (a `DbModule` provides the typed
Drizzle client over a `pg` Pool; migrations via `drizzle-kit`). Upgrades over the old app: **argon2**
(not bcrypt) + **JWT+refresh**.

- **AuthModule** — argon2, JWT access (~15m) + refresh (~7d, hashed on User for rotation), `JwtAuthGuard`,
  `RolesGuard` + `@Roles` (enum), `WsAuthService` for upgrade-time auth + stream tickets.
- **CryptoService** (in EnvVarsModule) — AES-256-GCM, master key from `WILLY_MASTER_KEY` (validate on
  boot), per-secret 12-byte nonce, stored `{cipherText,nonce,authTag,keyVersion}`; **decrypt only at
  injection**; log scrubber.
- **DockerModule/DockerService** — typed dockerode wrapper over `tcp://docker-socket-proxy:2375`:
  buildImage(+followProgress), createContainer/start/stop/remove, inspect (health + edge IP), logs
  (+`modem.demuxStream` for non-TTY), exec interactive TTY (+resize), network ensure/connect, scoped
  prune, image remove, ping, inspectVolume.
- **TraefikModule** — `LabelGeneratorService` (literal `traefik.http.routers.<name>.*` /
  `services.<name>.*`, sha-namespaced during swap, multi-domain OR rule, **no host ports**;
  **skipped entirely for WORKER/CRON**) + `DynamicConfigService` (file-provider YAML for
  middlewares/dashboard/custom domains).
- **DeploymentsModule** — CRUD + actions (deploy/redeploy/stop/start/rollback), `DeploymentStateMachine`,
  owns `activeReleaseId`. Branches behavior by `DeploymentType`.
- **BuildModule** — `BuildQueue` iface + `InProcessBuildQueue` (semaphore + per-deployment mutex +
  coalescing of superseded queued jobs), `GitService` (shallow clone, SHA resolve, **SSRF URL
  validation**, **per-deployment `GitCredential` for private repos** — GitHub App tokens / deploy key /
  PAT), `BuildOrchestrator` (state machine), strategies `NixpacksStrategy`/`DockerfileStrategy`/
  `ComposeStrategy`. Build-arg (`BUILD`/`BOTH`) vs runtime env (`RUNTIME`/`BOTH`) kept on separate paths.
- **DomainsModule** + **DnsModule** — domain attach/detach + cert status; `OvhClient` (signed requests,
  `/auth/time`, record CRUD + `/refresh`, zone-scoped token).
- **EnvVarsModule** — set/delete/listMasked/`resolveForInjection(scope)`; reveal endpoint (audited).
- **WebhooksModule** — `POST /webhooks/github/:deploymentId`, **raw-body HMAC-SHA256** (constant-time),
  event=`push` + ref filter, `autoDeploy` gate, enqueue + coalesce.
- **ConsoleModule** — WS exec TTY (hijacked duplex, resize), JWT at upgrade, `CONSOLE_OPEN` audit.
- **LogsModule** — build-log SSE + runtime-log SSE/WS (demux non-TTY), tail + history + backfill.
- **DatabasesModule** — managed DB lifecycle: create/start/stop/remove an engine container (Postgres/
  MySQL/Redis/Mongo) on a private network with a named volume + generated creds (encrypted); attach/detach
  to deployments (injects the connection string into runtime env + triggers a redeploy); exposes managed
  DBs as first-class backup targets (delegates to BackupsModule). Reconciled on boot like deployments.
- **BackupsModule** — volume-tar / pg-dump / s3-sync via one-shot helper containers; restore lifecycle
  (stop writers → optional pre-restore snapshot → restore → start → healthcheck → rollback-on-failure);
  `@nestjs/schedule` cron schedules; retention sweep; offsite push.
- **AuditModule**, **HealthModule** (`/health`, `/health/ready` = DB + docker ping),
  **ReconcileModule** (`OnApplicationBootstrap`: mark interrupted builds, recreate missing active
  containers, remove stale, re-sync Traefik file config — **after** migrations).

### Build/deploy state machine
WEB health-checked swap: `QUEUED → CLONING → BUILDING (image willy/<name>:<sha>) →` start new sha-named
container on `willy_edge` with labels+runtime env+limits `→ HEALTHCHECKING` (container health + internal
HTTP probe at `healthCheckPath`; old still serving) `→` cutover (point Host rule at new) `→ LIVE` (set
`activeReleaseId`) `→` stop+remove old (`SUPERSEDED`) `→` scoped cleanup (keep last N images). On
health-check failure: remove the new container, keep old live, mark `FAILED`. **Rollback** = start prior
`:<oldSha>` image + swap (no rebuild). **WORKER/CRON**: same up to BUILDING, then run with no labels/edge;
swap = stop-old/start-new. **Compose**: generate `willy.override.yml` (attach `composeWebService` to
external `willy_edge` + labels, **strip/reject host `ports:`**), `docker compose -p willy_<name> up -d
--build` (brief interruption), health-poll.

### Key backend gotchas
socket-proxy flags (`POST=1`,`EXEC=1`,`BUILD=1`,`CONTAINERS/IMAGES/NETWORKS/VOLUMES/INFO/PING=1`;
swarm/system/secrets off; never host-publish 2375) · Traefik router name uniqueness · build-arg vs
runtime env · compose host-port stripping · dockerode TTY (raw) vs non-TTY (demux) · webhook raw body
before JSON parse · `drizzle-kit migrate` as a one-shot entrypoint step (not racing in-process) · never global
prune (keep-N + dangling-only) · **acme.json + master key are catastrophic to lose** (back up both).

---

## Web dashboard (`frontend/`) — React + Vite + **MUI 9**

Stack: Vite 6 + React 19 + TS, **MUI 9** + emotion (latest as of Apr 2026; unifies Material UI + MUI X —
gives us MUI X DataGrid/Scheduler on the same version line for the deployments table & cron views),
`react-router-dom` v7 data-router,
**TanStack Query v5** over an `openapi-typescript-codegen`-generated client, `react-hook-form` + `zod`,
**notistack** toasts, **xterm.js** console, **SSE** for log tails / status bus + **WebSocket** for the
interactive console (auth via short-lived **stream ticket** from `POST /streams/ticket`, since
EventSource/WS can't set headers).

Routes: `/login`; `/deployments` (overview grid + StatusBadge); `/deployments/new` (7-step wizard,
**branches by deployment type**); `/deployments/:id` detail tabs → Overview, Build logs, Runtime logs,
Console, Env, Domains, DNS, Backups, Settings/DangerZone (Domains/DNS hidden for WORKER/CRON);
`/domains`, `/backups`, `/audit`, `/settings` (+ `/settings/users`, OVH creds status, base domain).

Key components: `LogViewer` (virtualized, ANSI, follow-tail, search, load-earlier, reconnect+backfill);
`Console` (xterm + FitAddon + resize frames over WS); `EnvVarEditor` (MUI table + Monaco bulk, mask/
reveal-audited, Save / Save&Redeploy); `DeployActions` (optimistic stop/start, job-based deploy/rollback
with `BuildProgress` stepper fed by SSE); `ConfirmDialog` (typed-name for destructive ops);
`StatusBadge`. Cross-cutting: `RequireAuth` + 401 interceptor + silent refresh; `QueryBoundary`
(Suspense+ErrorBoundary); reconnecting WS factory (backoff+jitter+heartbeat); platform-wide status SSE
(`/events/stream`) → `queryClient.setQueryData` to avoid N pollers.

---

## Traefik / routing (`routing/`) — `traefik:v3.7`

**Per-domain certs across any OVH zone is the primary model** (apps aren't confined to the panel zone).
Static `routing/traefik.yml`: entrypoints `web:80`→redirect→`websecure:443`; providers docker
(`exposedByDefault=false`, endpoint `tcp://docker-socket-proxy:2375`, network `willy_edge`, watch) +
file (`/etc/traefik/dynamic`); `certificatesResolvers.ovh.acme` DNS-01 (OVH provider, resolvers
`1.1.1.1`/`8.8.8.8`, `propagation.delayBeforeChecks 30s`, **staging caServer first**); `api.dashboard
true insecure:false`. OVH creds + `OVH_PROPAGATION_TIMEOUT` as Traefik env. Each WEB router carries
`tls.certresolver=ovh`, so Traefik issues a cert **per FQDN on demand** via OVH DNS-01 — works for any
zone the OVH token controls.

**Optional per-zone wildcard optimization:** for a zone with many subdomains (e.g. the panel zone
`willy.naucto.net`), pre-declare a `tls.domains` entry (main `willy.naucto.net` + san `*.willy.naucto.net`)
so those subdomains resolve to one cert and skip per-host challenges. Willy maintains these wildcard
entries via the file provider as zones accumulate apps; everything else falls back to per-domain certs.

Per-WEB-container labels (literal): `traefik.enable=true`, `traefik.docker.network=willy_edge`,
`routers.<name>.rule=Host(\`<app-fqdn>\`)`, `.entrypoints=websecure`, `.tls=true`, `.tls.certresolver=ovh`,
`.service=<name>`, `.middlewares=sec-headers@file`, `services.<name>.loadbalancer.server.port=<PORT>`.
File-provider dynamic: `middlewares.yml` (sec-headers HSTS, panel-auth basicAuth, rate-limit),
`dashboard.yml` (`traefik.<panel-zone>` → `api@internal` behind panel-auth), `wildcards.yml` (managed
per-zone wildcard `tls.domains`). Panel routers on willy-web/willy-server (`willy.naucto.net` + API under
`/api` or `api.` subdomain); SSE/WS pass through (no buffering middleware on those routers).

**Domain validation ("OVH perimeter"):** at domain-add, Willy resolves the FQDN's registrable zone and
checks it exists in the OVH account / token scope; if not, reject (or warn) since DNS records + DNS-01
can't be automated outside it.

Gotchas: router/service name uniqueness · a wildcard covers **one** label only (no `a.b.zone`; apex via
`main`) → deeper depths need their own cert · staging→prod = change caServer **and delete acme.json**
then restart · LE limits (50 certs/registered-domain/wk → prefer per-zone wildcards for high-subdomain
zones; per-domain certs for the long tail; **always validate on staging**) · OVH propagation slow
(generous timeout) · acme.json **chmod 600** or Traefik aborts.

---

## Backups (`backend/` BackupsModule) — containerize the team's existing approach

Reuses `~/Scripts/nc_db_backup.sh` (volume mountpoint tar) and `nc_s3_backup.sh` (aws s3 sync) but runs
**on the host via the control plane** (no SSH) by mounting volumes into one-shot helper containers:
- **volume-tar**: `docker run --rm -v <vol>:/data:ro -v willy_backups:/backup alpine tar -czf
  /backup/<id>.tar.gz -C /data .` (replaces `docker volume inspect {{.Mountpoint}}` + `cp -a`). Restore:
  stop writers → wipe+extract → start writers.
- **pg-dump**: `postgres:<major>` helper `pg_dump -Fc` / `pg_restore --clean --if-exists` (version-safe,
  unlike raw data-dir tar).
- **s3-sync**: `amazon/aws-cli s3 sync` (+`--endpoint-url` for non-AWS, reusing repo `S3_ENDPOINT`).
Storage on `willy_backups` volume (`/<deploymentId>/<kind>/<id>`), optional offsite S3 push, sha256 +
size + `Backup` row; `BackupSchedule` cron via `SchedulerRegistry`; retention keep-N sweep; single-flight
per target; typed-name confirm + server-side `confirm` flag; everything audited.

---

## Remote provisioner (`scripts/provision.sh` + `scripts/willy_deploy.sh`)

Two POSIX-sh files (port the `install.sh` idioms `sv_require`/`sv_try`/`sv_question`/`sv_lock`,
`/dev/urandom`/`openssl rand` secrets, verb dispatch). `provision.sh` runs on the operator laptop,
preflights, `scp`s `willy_deploy.sh` + a generated `.env`, runs it via `ssh … 'sudo sh -s'`. Verbs:
`provision` (default), `upgrade`, `doctor`, `backup`. (Ansible/cloud-init/Go-CLI evaluated and rejected
for a one-host op; revisit Ansible past ~3 hosts.)

- **Inputs** (flags + env-only for secrets): `--host/--ssh-user/--ssh-key/--ssh-port`, `--base-domain`,
  `--panel-subdomain` (default `panel`), `--acme-email`, `--ovh-endpoint` + `OVH_APPLICATION_KEY/SECRET`
  + `OVH_CONSUMER_KEY` (env only), `--admin-email` + `WILLY_ADMIN_PASSWORD?` (gen+print-once if absent),
  `--repo/--ref`, `--ca staging|prod` (default staging→auto-flip).
- **Preflight**: local tools; OVH creds present; SSH reachable + non-interactive sudo; Debian ≥12
  (Ubuntu path branch); **DNS resolves panel/apex/wildcard-canary → host IP**; ports 80/443 free; disk
  ≥5GB; clock skew.
- **Host setup (fresh Debian)**: `DEBIAN_FRONTEND=noninteractive`; apt update/upgrade; base pkgs
  (`ca-certificates curl gnupg git ufw fail2ban unattended-upgrades jq openssl`); **Docker official
  repo** (keyring + `$VERSION_CODENAME`, debian vs ubuntu path) + `docker-ce docker-ce-cli containerd.io
  docker-buildx-plugin docker-compose-plugin`; enable docker; `/etc/docker/daemon.json` log rotation
  (first run only); `willy` user in `docker` group (group-refresh gotcha → run docker steps as root /
  fresh session); ufw deny-default + allow 22/80/443; unattended security upgrades (no surprise reboot);
  journald size cap; swap if RAM<2GB; SSH hardening drop-in (password off) **after** key-auth verified.
- **Secrets + .env**: idempotent `set_secret KEY GEN` (preserve existing, never clobber `WILLY_MASTER_KEY`/
  DB pw/JWT on re-run); generate via `openssl rand`; atomic mode-600 willy-owned `.env`; never echo
  secrets; admin password hashed by the app's own seed (algorithm parity), printed once only if generated.
- **Deploy**: clone/reset `/opt/willy@ref`; ensure `letsencrypt/acme.json` mode 600; `docker compose
  pull` (+`build`); `up -d`; wait healthchecks (postgres/willy-server/traefik); `drizzle-kit migrate`;
  **idempotent admin upsert seed**; cert poll on **staging** then **flip to prod** (rewrite caServer,
  empty acme.json, restart traefik, verify issuer ≠ STAGING via `openssl s_client`).
- **upgrade**: lite preflight → git reset → `compose pull` → `up -d --remove-orphans` (only changed
  services bounce; tenant apps untouched) → `drizzle-kit migrate` → health + doctor. Remote lockfile.
- **doctor** (read-only, non-zero on any ✗): DNS, OVH API reachability, container health, cert
  perms/issuer/expiry, disk + `docker system df`, ports, user/group, time skew, `.env` sanity.
- **Security**: SSH key-only; OVH token **zone-scoped** (`GET/POST/PUT/DELETE /domain/zone/<zone>/*` +
  `/refresh`); secrets in `/opt/willy/.env` (600) + acme.json (600); `backup` verb bundles pg_dump + `.env`
  + acme.json off-host (encrypt); ufw; docker-socket-proxy is sole socket mount.

### Operator runbook (the "almost one shot")
Concrete Naucto target: **panel** at `willy.naucto.net`. Apps deploy to **any** FQDN in an OVH-managed
zone (e.g. `app.willy.naucto.net`, `crawler.naucto.net`, `staging.someproduct.io` if that zone is in
OVH) — chosen per app in the UI, no reprovision needed.
1. Provision a fresh Debian 12 VPS; install your SSH key for root (or passwordless-sudo user); note IP.
2. Create an OVH API token scoped to **every zone you'll deploy into** (at minimum `naucto.net`):
   `GET/POST/PUT/DELETE /domain/zone/<zone>/*` + `/refresh`. Save app key/secret + consumer key.
3. Point the **panel** record: `willy.naucto.net A <IP>` (and, if you want a fast wildcard for
   subdomains of that zone, `*.willy.naucto.net A <IP>`); wait until `dig +short willy.naucto.net` == `<IP>`.
   Per-app A/CNAME records are created later **from the panel** when you add a domain to a deployment.
4. One command: `OVH_APPLICATION_KEY=… OVH_APPLICATION_SECRET=… OVH_CONSUMER_KEY=… ./scripts/provision.sh
   --host <IP> --ssh-user root --base-domain willy.naucto.net --panel-subdomain "" --acme-email you@…
   --ovh-endpoint ovh-eu --admin-email you@…`
5. Log in at `https://willy.naucto.net` with the printed one-time admin password; change it. Then deploy
   apps to whatever OVH-managed domains you like.

(`--base-domain` only sets the **panel**'s home + the default zone offered first in the new-deployment
wizard; apps are free to use other OVH zones.)
→ 3 manual setup steps + 1 command; only DNS propagation + human OVH-token creation prevent literal one-shot.

---

## Version switching (health-checked swap, brief interruption)

**Not strict zero-downtime — a brief interruption is acceptable** (per user). All triggers (manual deploy,
webhook auto-deploy, rollback) use one path: build/resolve the new immutable image → start the new
container → **health-check it** (container health + internal HTTP probe at `healthCheckPath`) → flip the
Traefik Host router to the new container → stop + remove the old. The only safety property kept: the **old
version keeps running until the new one passes health-check**, so a failed build/boot never takes down the
working version (on failure: discard the new container, keep the old, mark the deploy failed). The sole
"downtime" is the sub-second route flip — no side-by-side dual-serving, no connection draining, no
zero-failed-requests guarantee. WORKER/CRON have no inbound traffic (stop-old/start-new). Compose stacks
recreate in place.

---

## Quality, tooling & CI (maintainability-first)

Verified-current stack (June 2026): **Node 24 LTS**, **Traefik v3.7.x**, **MUI 9**, **Drizzle**.

- **Lint + format:** **Biome** — one fast tool + one config for `backend/` and `frontend/`, replacing
  ESLint+Prettier. (If a React-hooks-specific rule gap shows up, add a thin ESLint layer only for
  `eslint-plugin-react-hooks`; otherwise Biome only.) Enforced in CI + editor.
- **Types:** strict `tsconfig` (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`,
  `noImplicitOverride`); `tsc --noEmit` typecheck gate.
- **Tests:** **Vitest** for unit + integration (backend + frontend). **Testcontainers** spins real
  Postgres (Drizzle integration) and exercises the Docker engine via the socket-proxy for build/run/exec
  tests. **Playwright** for e2e against the running panel (login → create deployment → stream logs →
  console → rollback). Unit targets: CryptoService round-trip/tamper, Traefik label generation, webhook
  HMAC, BuildQueue serialization/coalescing, OVH signature, domain/zone (OVH-perimeter) validation.
- **Pre-commit:** **lefthook** runs Biome + typecheck + affected unit tests on staged files.
- **CI (GitHub Actions):** matrix jobs — install → Biome → typecheck → unit → integration
  (Testcontainers) → build images → Playwright e2e (compose-up an ephemeral stack) → on tag, build &
  push images to `ghcr.io`. Branch protection requires green CI.
- **Commits/PRs:** `[AREA] [ACTION] What it does` format (commit-msg hook); PR template; CODEOWNERS.
- **Comment policy** (per user): comment the **why**, never the obvious **how**; keep comments short or
  omit them; only when logic is ambiguous, a few lines explaining the reasoning/trade-off. Codified in
  `AGENTS.md` so humans and agents follow it.
- **Cleanup discipline:** no dead code/unused deps (Biome + `knip` optional in CI); scoped Docker cleanup
  (keep-N images per deployment, dangling-only prune) — never global prune.

---

## Local development & testing

Willy runs end-to-end on a laptop with **no VPS, no OVH, no public DNS** — the default way to develop
and to "test it out".

- **Domains:** base domain `localhost` → panel at `willy.localhost`, deployed apps at `<app>.localhost`.
  Browsers resolve `*.localhost` to `127.0.0.1` automatically (no `/etc/hosts` edits needed).
- **Certs:** `docker-compose.local.yml` swaps the OVH/ACME cert resolver for a **mkcert**-generated
  wildcard cert for `*.localhost`, mounted into Traefik via the file provider → browser-trusted HTTPS
  with no warnings. (Fallback: Traefik default self-signed if mkcert isn't installed.) The OVH/DNS-01
  code path is simply not exercised locally.
- **Parity:** same Traefik + docker-socket-proxy + Postgres + willy-server/web containers and the same
  build/deploy/swap/console/logs/backup logic, run against the **local Docker daemon**. Deploying apps
  through Willy locally behaves exactly like prod (minus public reachability).
- **Run:** `make dev` = `docker compose -f docker-compose.yml -f docker-compose.local.yml up`. Two flavors
  via the override: **parity** (built images) and **watch** (Vite dev server + `nest start --watch`,
  source-mounted, for fast iteration) — pick with a compose profile.
- **Seed:** local bootstrap auto-creates an admin (`admin@willy.localhost` / known dev password) and the
  mkcert cert, so `make dev` → log in immediately.
- **Advanced (optional):** to test the *real* OVH cert path locally, DNS-01 issues certs without inbound
  reachability, so you can point a real OVH FQDN at `127.0.0.1` via `/etc/hosts` and get a genuine LE
  **staging** cert — documented but not the default.
- **Tests run locally too:** `make test` (Vitest), `make e2e` (Playwright drives `willy.localhost`),
  integration via Testcontainers — all without a VPS.

This is delivered in **Phase 1** so every later phase is built and tested locally first; the VPS is for
integration/regression (Phase 8) and real deployments.

---

## Phased implementation (each phase ≈ one working session)

Phases are ordered for a thin end-to-end slice first, then layered. Later phases can be separate
sessions; a phase's "Done when" is its handoff checkpoint. Detailed per-module task checklists live in
the agent deep-dives captured in `docs/plans/` appendices at implementation start. **Every phase ships
with its own tests** (Vitest unit/integration for new logic; Playwright e2e once the UI exists) and must
pass Biome + typecheck + CI before it's "done" — testing is not a deferred phase.

### Phase 0 — Reset, rename, scaffolding, tooling & CI *(session 1)*
- [x] `gh repo rename Willy` — **stays under the `Naucto` org → `Naucto/Willy`** (rename keeps the owner;
      GitHub auto-redirects the old URL). Update the `origin` remote to `git@github.com:Naucto/Willy.git`;
      rename local folder `…/Naucto/Docker-Synchronizer` → `…/Naucto/Willy`.
- [x] **Clean repo reset (decision #7):** optionally push a `pre-willy` safety tag/branch to the remote,
      then start fresh — wipe the old tree (including the `dev-environment` submodule + `.gitmodules`),
      no `legacy/` kept. (Either `git checkout --orphan` a new root, or remove all tracked files in one
      reset commit — pick at impl time; net result is Willy as the going-forward history.)
- [x] Create layout: `AGENTS.md` (+ `CLAUDE.md` symlink; **encode the comment policy + conventions**),
      `README.md`, `docs/`, `docs/plans/0001-willy.md` (copy this plan), `backend/`, `frontend/`,
      `routing/`, `scripts/`, `.env.example`.
- [x] **Tooling baseline:** Node 24 LTS pin (`.nvmrc`/engines), workspace setup, **Biome** config (shared),
      strict `tsconfig` bases, **Vitest** config (backend + frontend), **lefthook** pre-commit
      (Biome+typecheck+affected tests), Conventional-Commits + PR template + CODEOWNERS.
- [x] **CI (GitHub Actions):** install → Biome → typecheck → unit → integration (Testcontainers) → build
      → Playwright e2e → (on tag) push images to `ghcr.io`; enable branch protection.
- [x] Author infra `docker-compose.yml` (traefik **v3.7**, docker-socket-proxy, willy-server, willy-web,
      postgres; `willy_edge` external network; `willy_backups`/`letsencrypt` volumes; healthchecks).
- **Done when:** repo renamed everywhere, empty compose stack builds, docs + tooling + green CI pipeline
      committed (a trivial test passes through the full pipeline).

### Phase 1 — Provisioner + Traefik edge (staging certs) *(session 2)*
- [x] `routing/traefik.yml` + `routing/dynamic/` (staging ACME, OVH DNS-01 per-domain + optional
      panel-zone wildcard `tls.domains`, file middlewares, secured dashboard); acme.json 600 init.
- [x] `scripts/provision.sh` + `scripts/willy_deploy.sh` (preflight, host setup, secrets/.env, compose
      up, cert staging poll). `doctor` verb.
- [x] **Local mode:** `docker-compose.local.yml` (mkcert `*.localhost` cert via file provider, no OVH),
      `make dev` (parity + watch profiles), local admin/cert seed.
- **Done when:** `make dev` serves the empty panel at `https://willy.localhost` with a browser-trusted
      (mkcert) cert; AND `./scripts/provision.sh` on a throwaway Debian 12 VPS serves the panel at the
      configured base domain (e.g. `willy.naucto.net`) over a valid LE-**staging** cert; `doctor` all green.

### Phase 2 — Backend foundation: auth, crypto, docker, health *(session 3)*
- [x] NestJS scaffold (aliases, deps incl. drizzle-orm/drizzle-kit/pg/dockerode/argon2/simple-git/ovh/ws),
      `main.ts` (ValidationPipe, swagger, WS upgrade router, raw-body for `/webhooks`), `DbModule` (Drizzle
      client over pg Pool), full Drizzle schema (`backend/src/db/schema/*`) + initial generated migration,
      ConfigModule env validation.
- [x] CryptoService (+tests), AuthModule (argon2, JWT+refresh, guards, roles, WsAuth, stream tickets),
      UsersModule, admin bootstrap seed, HealthModule, AuditModule, DockerService (+`DOCKER_IT` tests).
- **Done when:** login works over the provisioned host; `/health/ready` confirms DB + docker-socket-proxy.

### Phase 3 — Thin deploy slice: Dockerfile WEB + WORKER, env, logs *(session 4)*
- [x] EnvVarsModule (encrypted, masked, scope split), DeploymentsModule CRUD + state machine,
      BuildModule (`InProcessBuildQueue`, GitService+SSRF, BuildOrchestrator, DockerfileStrategy),
      TraefikModule LabelGenerator (WEB only), LogsModule (build+runtime SSE). Private-repo auth via
      `GitCredential` (start with deploy key / PAT; GitHub App is the recommended target).
- [x] `POST /deployments/:id/deploy` end-to-end for **WEB** (Dockerfile→container on `willy_edge`+labels)
      and **WORKER** (no labels/edge). Accept brief downtime this phase.
- **Done when:** deploy a real Dockerfile WEB repo to a subdomain (staging cert) AND a domain-less WORKER,
      both from the API; env injected; build logs stream. ✅ Verified locally: `docker-hello-world` WEB
      reached LIVE and served `200` over HTTPS at `hello.localhost` with `FOO` injected; a WORKER reached
      LIVE on `bridge` with zero Traefik labels; build logs streamed over SSE. Build runs through the
      legacy builder (`version: "1"`) since the socket-proxy blocks BuildKit's `/session`.

### Phase 4 — Web dashboard (MUI) MVP *(session 5)*
- [x] `frontend/` Vite+React+MUI 9; generated API client; QueryProvider; AuthProvider/RequireAuth; AppShell;
      Login; DeploymentsOverview; DeploymentDetail (Overview/Build logs/Runtime logs/Env tabs);
      CreateDeploymentWizard (type-branching); DeployActions; LogViewer; EnvVarEditor.
- [x] Route willy-web/willy-server via Traefik labels (panel base domain + API under `/api`).
- **Done when:** full create→deploy→watch-logs→edit-env loop works in the browser for WEB and WORKER.
      ✅ Backend emits an OpenAPI doc (`backend/src/openapi.ts`, explicit `@ApiProperty`/`@ApiBody`/`@ApiParam`
      since the build is plain `tsc` — no swagger transformer); `frontend` generates `src/api/schema.d.ts`
      (openapi-typescript) and a typed `openapi-fetch` client with token refresh + a fetch-based SSE reader.
      Stack verified locally: SPA served at `https://willy.localhost` with deep-route fallback; the
      create→set-env→list→delete loop works through the panel's same-origin `/api`. Used a plain MUI Table
      (not X DataGrid) for the overview MVP.

#### Phase 4 — UX follow-ups (user feedback, 15-06-2026)
Quick frontend-only polish landed in commit `ab3e96b` + the slider rework; backend-touching items deferred.
- [x] **(4) Memory limit slider** on the create form — boxed to match the dialog, with tick marks
      ("tracks": Off/1G/2G/4G) and a value-label, plus the numeric MB field. *(FE-only, done)*
- [x] **(5) Env var validation feedback** — warning toast when saving a var with no key. *(FE-only, done)*
- [x] **(6) Logs empty-state** — Build/Runtime tabs explain "no release / not running" instead of erroring. *(done)*
- [x] **(7) Contextual action buttons** — Deploy/Stop/Start shown by state. *(done)*
- [x] **(8) Clickable rows + action menu** — row navigates; kebab menu on list + detail. *(done)*
- [x] **(11a) Strategy-aware create form** — Dockerfile path only for DOCKERFILE; health-check hidden for
      COMPOSE (FE part done). Compose file/web-service inputs + backend support → **Phase 6**.
- [x] **(1) Edit deployment settings** post-create — `PATCH /deployments/:id` + Settings tab (incl.
      autoDeploy toggle and webhook secret management). *(done in Phase 5)*
- [x] **(10) Infer health check for Compose** — the probe is derived from the compose service's own
      healthcheck + HTTP probe; the field is hidden for COMPOSE. *(done in Phase 6)*
- [x] **(11b) Compose create fields** — `composeFilePath` / `composeWebService` inputs +
      CreateDeploymentDto/UpdateDeploymentDto support. *(done in Phase 6)*
- [ ] **(12) Compose multiple (sub)domains** — the compose override already builds a multi-domain Traefik
      OR-rule from `allDomains()`, but the UI still sets a single primary domain. Needs a multi-domain
      editor. → **future phase** (DomainsModule).
- [ ] **(13) Dynamic memory-limit range** — the slider max (currently hardcoded 4G) and marks should reflect
      the target host's RAM, surfaced from the provision step (host capacity) or the docker `INFO` endpoint,
      rather than a fixed cap. → **Phase 5/7** (host capacity in provision/doctor + expose to the panel).
- [x] **(3) Domain detection** — the deployment **DomainPicker** lists auto-discovered OVH zones and
      composes the FQDN from a chosen zone + subdomain instead of free-text. *(done in Phase 6)*
- [x] **(2) OVH DNS record management** in-panel (list/add/edit/delete + bulk delete + host-IP prefill).
      *(done in Phase 6, live on the VPS)*
- [ ] **(9) User account management** — `UsersService` already has `create`/`list`; expose a controller +
      `/settings/users` UI (invite/role/disable). → **Phase 7** (hardening).

#### Feedback batch round 2 (user testing, 15-06-2026)
- [x] **Restart action** (POST `/deployments/:id/restart`) — recreates the active container, picking up
      env/config changes; available as a button + menu item.
- [x] **Per-button spinners** on deployment actions (clicked action shows a spinner, all disabled while busy).
- [x] **Action buttons cleanup** — dropped the redundant kebab on the single-deployment page (buttons only);
      kept the kebab on the deployments list.
- [x] **Domain visible + editable** — shown in Overview, editable in Settings (`PATCH` sets the primary
      domain; takes effect on next deploy/restart). *(Full multi-zone OVH DNS management still pending.)*
- [x] **Session-expiry redirect** — a failed token refresh clears the session and routes to /login;
      hardened the auth fetch retry path (addresses the intermittent "failed to fetch" on actions).
- [x] **Login redesign** — background photo + blue tint; host/build info (panel version, distro/kernel,
      commit) in the lower-right from public `/api/system/info`. *(distro reflects the panel container's
      base image; true host distro needs the provisioner to pass it — minor follow-up.)*
- **Not a Willy change:** "support `localhost` in containers" — a container's own `HEALTHCHECK` is baked
      into its image and resolves `localhost` per that image's libc/resolver (busybox → IPv6 `::1`). Willy
      can't rewrite that; the fix is in the app/image (probe `127.0.0.1`, or bind dual-stack `::`).

### Phase 5 — Health-checked swap, Nixpacks, webhooks, reconciliation *(session 6)*
- [x] Health-checked swap state machine (per-release container, health probe with old still serving, route
      flip, remove old, keep-N cleanup; old kept on failure) for WEB; stop/start/redeploy. NixpacksStrategy.
      WebhooksModule (raw HMAC + ref filter + autoDeploy toggle, coalescing) + webhook secret UI.
      ReconcileModule on boot.
- **Done when:** git push auto-deploys with only a brief interruption; a failing build leaves the old
      version serving; bad HMAC rejected; host reboot self-heals via reconciliation.
      ✅ Verified locally: swap cuts over only once the new container is **Docker-healthy** (Traefik refuses
      to route a "starting" container, so cutover waits — newer router gets a *lower* priority so the old
      keeps serving until removed); a deploy that fails health left the old serving (state DEGRADED, release
      FAILED); webhook accepted a correctly-signed `push` on the matching ref, rejected a bad signature and a
      non-matching ref; killing the app container + restarting the server recreated it via reconciliation.
      Nixpacks installed in the image (generates the Dockerfile; build still goes through the socket-proxy).
      Also shipped follow-up **(1)**: `PATCH /deployments/:id` + a Settings tab (edit fields, autoDeploy,
      webhook URL + generate/rotate secret).

### Phase 6 — Rollback, compose, multi-zone domains/DNS, console *(session 7)*
- [x] Rollback (no rebuild) — relaunch a prior release's image through the health-checked swap; swap
      priority now derives from launch time so rollback supersedes correctly. ✅ Verified (A→B→rollback A).
- [x] ConsoleModule (xterm WS exec) + Console UI tab — signed stream ticket, raw WS upgrade, dockerode
      exec TTY bridge. ✅ Verified (valid ticket runs a command; bad ticket → 401).
- [x] ComposeStrategy — `docker compose -p <project> up -d --build` via the docker CLI + compose plugin
      in the image, builds on the **legacy builder** through the socket-proxy (resolved the BuildKit
      concern: `DOCKER_BUILDKIT=0` → classic builder works). A generated `willy.override.yml` attaches the
      web service to `willy_edge` with Traefik labels (multi-domain OR-rule); teardown removes the stack by
      its compose-project label. Compose inputs (file path + web service) in create/settings; health-check
      inferred (the field is hidden — the web service's own healthcheck + HTTP probe gate it). ✅ Verified
      E2E with **Willy-Test-Compose**: both services healthy, `compose.localhost` → 200, `/api` proxied to
      the backend; stop/delete tears the stack down. *(Host-port stripping + per-stack image cleanup +
      env injection into compose services are noted limitations; rollback is N/A for compose.)*
- [x] **DnsModule** — pluggable `DnsProvider` interface with two implementations chosen at boot
      (`DNS_PROVIDER=ovh|local`, auto-detected from creds): **OvhDnsProvider** (signed OVH API, time-skew
      sync, zone auto-discovery via `GET /domain/zone`, per-zone record CRUD + refresh) and
      **LocalDnsProvider** (in-memory, seeded zones — local dev with no OVH account/public DNS). DNS editor
      UI: a **zone dropdown fed by auto-discovery** (reacts on pick, auto-selects the first zone), a
      records DataGrid with single + **multi-select bulk delete**, an add-record dialog that can pre-fill
      A/AAAA targets with the **host's public IP** (`GET /system/public-ip`), and a collapsible left
      **sidebar** (Deployments / DNS). Signer + local provider unit-tested; ✅ OVH signing verified
      against the real API; local provider verified E2E; ✅ live on the VPS (auto-discovers `naucto.net`).
      Not-configured returns a clean 503. *(Auto-discovery needs the OVH token to also grant
      `GET /domain/zone`.)*
- [x] **Dashboard polish (this session)** — deployment domain is chosen via a **DomainPicker** (zone
      grid + subdomain → FQDN, mirrors the release picker); collapsible sidebar (icons-only ↔ labels);
      opaque app bar; login screen disables sign-in + polls until the backend is reachable.
- [ ] **DNS zone registration** — adding/registering a *new* zone belongs in a dedicated config surface
      (settings page or modal), not the records view. **Follow-up — future phase.**
- [ ] **DomainsModule** deeper bits — auto-create the DNS record on domain attach + OVH-perimeter
      (zone-in-account) validation + per-domain DNS-01 cert issuance + optional per-zone wildcard. The
      domain *field* already drives routing; cert issuance needs real public DNS. **Follow-up — on the VPS.**
- [ ] **Flip Traefik to prod ACME** (delete acme.json). **Follow-up — VPS op.**
- [x] Test repos created: **Naucto/Willy-Test-Dockerfile** and **Naucto/Willy-Test-Compose** (private; make
      public or pass a git token to deploy them).
- **Done when:** rollback, compose stack, an app on a **non-panel-zone** OVH domain (record auto-created +
      per-domain **prod** cert), and live console all work. *(rollback, console, compose, and OVH DNS
      management all done + deployed to the VPS; remaining = per-domain **prod cert** issuance + the prod
      ACME flip, both VPS-only follow-ups needing real public DNS.)*

### Phase 7 — Managed databases, backups, CRON type, hardening, docs *(session 8)*
- [ ] **DatabasesModule** + UI: one-click managed DB (Postgres/MySQL/Redis/Mongo) on a private network +
      volume + encrypted creds; attach/detach to deployments (inject connection string + redeploy);
      reconcile on boot.
- [~] **BackupsModule** (volume-tar / pg-dump / s3-sync + restore lifecycle + schedules + retention +
      offsite) + Backups UI, **with managed DBs as first-class backup targets**.
  - [x] **Slice 1 — volume-tar (done):** one-shot `alpine tar` helper writes archives to the pinned
        `willy_backups` volume (mounted into willy-server for sizing/checksum/stream/delete); single-flight
        `BackupQueue`; `GET /backups` + `/backups/volumes`, `POST /backups`, `DELETE /backups/:id`,
        `GET /backups/:id/download` (StreamableFile). Backups page (DataGrid + create dialog + download/
        delete) and a sidebar entry. ✅ Verified E2E locally: backed up `willy_postgres-data` → SUCCESS
        (6.95 MB), downloaded a valid gzip of the PG data dir, delete removes file + row.
  - [ ] **Slice 2+:** pg-dump (`pg_dump -Fc` helper on the DB network) + s3-sync (offsite) + restore
        lifecycle + `@nestjs/schedule` schedules + retention sweep.
- [ ] CRON deployment type (scheduled one-shot). Resource limits enforced everywhere; scoped disk
      cleanup; log retention; master-key rotation path; e2e suite; `provision.sh upgrade`/`backup` verbs
      finalized; AGENTS.md/README/runbook complete.
- **Done when:** create a managed Postgres, attach it to an app (app reads the injected conn string),
      back it up + restore round-trip; scheduled backup fires; CRON job runs on schedule; resource limits
      enforced; `upgrade` path verified.

### Phase 8 — Migrate real workloads (regression) *(session 9 — collaborative with the user)*
> **Partially user-assisted.** This phase carries a lot of real environment + secrets + production config
> (DB creds, JWT/master keys, S3, OVH zones, production domains, the actual `.env` corpus). **Scope and
> sequence it together with the user in a dedicated working session** before executing — do not assume the
> config; gather it interactively.
- [ ] Redeploy the **Naucto app** (NestJS backend + Vite frontend from `github.com/Naucto/Dev-Environment`)
      through Willy on its production domain, replacing the old synchronizer end-to-end. Host the
      GitHub↔Jira synchronizer and Epitech repository-crawler as WORKER deployments (their code rewrites
      live in their own repos — out of scope here).
- **Done when:** old synchronizer is fully decommissioned; all three workloads run under Willy.

---

## Out of scope (v1)

- **Multi-host / clustering** — single-host only. Likely not needed now or even later; revisit only if
  real demand appears (would then need an image registry + scheduler + shared state).
- **Non-OVH DNS providers** — OVH is the only DNS/ACME integration; other providers are out of scope.
- **Code rewrites of other Naucto services** (GitHub↔Jira synchronizer, Epitech repo-crawler) happen in
  **their own repos** — explicitly *not* part of Willy. Willy only needs to *host* them as WORKER
  deployments; nothing about those rewrites lands in this repo.

**Switching model (by choice, not a gap):** all version switches accept a **brief interruption** (the
sub-second Traefik route flip; compose stacks a bit more). Strict zero-downtime (side-by-side overlap +
connection draining) is intentionally **not** implemented. The kept safety property is that a broken
build never replaces a healthy running version (old stays up until new passes health-check).

## Verification (end-to-end)

Per-phase "Done when" above are the gates. Consolidated final checks:
- **Local:** `make dev` → panel at `https://willy.localhost` (browser-trusted mkcert cert); deploy a
  sample app to `app.localhost` and reach it over HTTPS — no VPS/OVH/DNS involved.
- CI is green: Biome + typecheck + Vitest unit/integration (Testcontainers) + Playwright e2e all pass.
- `curl -I https://willy.naucto.net` → 200, **trusted** (non-staging) cert; `docker network inspect
  willy_edge` shows Traefik + WEB apps only.
- Create WEB (Dockerfile + Nixpacks) and WORKER and CRON deployments from the UI; env injected (verify
  inside container); build+runtime logs stream; console runs a command.
- Apps reach **different OVH zones**: one under the panel zone (covered by the optional wildcard) and one
  on a separate OVH-managed domain (record auto-created + per-domain prod cert) — both serve over HTTPS.
- **Switching:** `git push` → HMAC-verified auto-deploy completes with only a brief interruption; a
  **broken build does not take down the running version** (old stays live until new is healthy); rollback
  restores the prior release; bad-HMAC POST rejected.
- Volume + pg_dump backup → destroy → restore → data intact; scheduled backup fires; retention prunes.
- Reboot host → reconciliation brings every deployment back; `docker inspect` shows resource limits.
- Fresh-VPS regression: `./scripts/provision.sh` from zero → working panel; then redeploy the Naucto app
  through Willy, decommissioning the old Python synchronizer.
